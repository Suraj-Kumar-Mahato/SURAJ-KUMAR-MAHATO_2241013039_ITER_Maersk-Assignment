# --- Choose one email backend ---
# SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_password
SMTP_TLS=true

# OR SendGrid
# SENDGRID_API_KEY=SG.your_key

# Recipients (comma separated)
RECIPIENTS=netops@example.com, noc@example.com

# Provider: json (simulator) or ndac_api
PROVIDER_TYPE=json
